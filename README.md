this is a project i made to see if i could load a 3d model with opengl. the repo does not contain most dependencies like glfw glm tinyobjloader or stb_image. if you really want to compile it on your computer you can read the CMakeLists.txt to figure out where to put stuff. for auto compilation run the au.sh if you're on linux, though be warned, i am a bad programmer so it might not work. 
